name: Ultroid Workflow

on:
    push:
    workflow_dispatch:
    
jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
       - name: Checkout
         uses: actions/checkout@main
       
       - uses: styfle/cancel-workflow-action@0.9.0
         name: "Cancel duplicate workflow runs"
         with:
            all_but_latest: true
            access_token: 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 
       - name: initialization
         continue-on-error: true
         run: |
           git config --global user.name rimuru-0
           git config --global user.email japaneseemail00@gmail.com
           git config --global credential.helper store
           echo "https://rimuru-01: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@github.com/rimuru-01/ultroid-termux" > ~/.git-credentials
       - name: cloning repo
         continue-on-error: true
         run: |
           git clone https://rimuru-01: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@github.com/rimuru-01/ultroid-termux ultroid
       - name: Running Ultroid
         timeout-minutes: 350
         continue-on-error: true
         run: |
           sudo sh action.sh
       - name: Loop workflow
         continue-on-error: true
         run: |
              git clone https://github.com/${GITHUB_REPOSITORY} loop
              cd loop || exit 1
              echo $(date -u +%T%P) >> loop.txt
              git add loop.txt
              git commit -m "Workflow : Loop $(date -u +%D-%T%P)"
              git push -q https://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@github.com/${GITHUB_REPOSITORY} HEAD:main

